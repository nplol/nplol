%section.posts
  %ul
  - @posts.each do |post|
    %li.post-container
      %article.post
        %h1.title
          = post.title
        %p
          =truncate_html(markdown(post.content), length: 1000, omission: " [...] #{ link_to 'Read more', post }")
