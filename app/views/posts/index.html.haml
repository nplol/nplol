%ul.posts
  - @posts.each do |post|
    - classes = post.popular ? 'popular' : 'normal'
    -	classes += post.top ? ' top' : ''
    %li.post{ class: classes }
      = link_to post do 
        = render post
