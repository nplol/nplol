%section.flash
  .message
    %i.fa.fa-2x.fa-ban
    %span= flash[:notice] if flash[:notice]

- if flash[:notice]
  :javascript
    $(document).ready(function() {
      window.app.emit('error')
    });
