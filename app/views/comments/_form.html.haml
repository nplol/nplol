= simple_form_for [comment.post, comment], remote: true do |f|
  - if logged_in?
    = f.input :text, placeholder: 'Write something clever', image: current_user.avatar
  - else
    %section.notice
      %span= t('login')
